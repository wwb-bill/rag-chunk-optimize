# rag-chunk-optimize

Auto-find optimal RAG chunking parameters. Grid-search across strategies, sizes, overlaps.

```bash
pip install rag-chunk-optimize
rag-chunk-optimize run corpus.json queries.jsonl
```

MIT
